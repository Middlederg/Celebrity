using System.Threading.Tasks;

namespace Celebrity.Domain
{
    //public class TimesUpService
    //{
    //    private readonly IDeckRepository deckRepository;
    //    private readonly GameFinder gameFinder;

    //    public async Task<Game> TimesUp(GameId gameId)
    //    {
    //        var game = await gameFinder.Find(gameId);
    //        var turn = game.MoveToNextTurn();
    //        var deck = new Deck(game.DeckConcepts);
    //        deck.VisibleConcept.Guess();
    //        await deckRepository.UpdateConcept(deck.VisibleConcept);


    //        game.CurrentTurn nturn .NextTurn
    //        teams.MoveToNextTeamTurn();
    //        if (!deck.IsFinished)
    //        {
    //            deck.NextConcept();
    //        }
    //    }
    //}
}
